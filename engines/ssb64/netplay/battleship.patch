diff --git a/CMakeLists.txt b/CMakeLists.txt
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -932,7 +932,9 @@ elseif (EMSCRIPTEN)
         -sINITIAL_MEMORY=256MB
         -sSTACK_SIZE=8388608
         -sFORCE_FILESYSTEM=1
-        -sEXPORTED_RUNTIME_METHODS=FS,callMain
+        # Emscripten 6 requires explicit Module heap exports. The browser shell
+        # and netplay controller bridge read these views, including after growth.
+        -sEXPORTED_RUNTIME_METHODS=FS,callMain,HEAPU8,HEAP32
         -sINVOKE_RUN=0
         -sEXIT_RUNTIME=0
         # ASSERTIONS=0 + -g1 for demo/production wasm: ASSERTIONS wraps the
diff --git a/port/gameloop.cpp b/port/gameloop.cpp
index 9b0e715..4b5e2ed 100644
--- a/port/gameloop.cpp
+++ b/port/gameloop.cpp
@@ -768,8 +768,17 @@ static void port_screenshot_maybe_capture(int frame)
 	}
 }
 
+extern "C" int port_netplay_enabled(void);
+extern "C" int port_netplay_before_frame(uint32_t tick);
+
 void PortPushFrame(void)
 {
+	/* Confirm every input before cheats, VI, controller reads or game logic. */
+	if (!port_netplay_before_frame((uint32_t)sFrameCount)) {
+		port_watchdog_note_frame_end();
+		return;
+	}
+
 	// Process cheats safely before the frame updates
 	lbBackupApplyCheats();
 
@@ -789,7 +798,7 @@ void PortPushFrame(void)
 #ifdef __EMSCRIPTEN__
 	/* Matchup card: hold the game (no VI tick, no coroutine resume) while
 	 * the shell shows who is fighting. The watchdog still sees frames. */
-	if (port_matchup_gate()) {
+	if (!port_netplay_enabled() && port_matchup_gate()) {
 		port_watchdog_note_frame_end();
 		return;
 	}
diff --git a/port/port.cpp b/port/port.cpp
index 24b78db..74f672b 100644
--- a/port/port.cpp
+++ b/port/port.cpp
@@ -46,6 +46,10 @@ extern "C" { extern int gPortProfEnabled; extern uint64_t gPortProfStageNs[8]; e
 #include "port_window_icon.h"
 #endif
 #ifdef __EMSCRIPTEN__
+extern "C" void port_netplay_init(void);
+extern "C" int port_netplay_enabled(void);
+extern "C" int port_netplay_wait_frame(uint32_t tick);
+extern "C" int port_get_frame_count(void);
 #include <emscripten.h>
 #include <emscripten/heap.h>
 
@@ -1389,6 +1393,13 @@ int main(int argc, char* argv[]) {
 
 #ifdef __EMSCRIPTEN__
 	fprintf(stderr, "SSB64[wasm]: entering PortInit\n");
+	port_netplay_init();
+	if (port_netplay_enabled()) {
+		/* A swap must not add another Asyncify wait inside the tick. The
+		 * online main-context clock owns pacing; the browser composites the
+		 * latest completed canvas independently. */
+		SDL_SetHintWithPriority(SDL_HINT_EMSCRIPTEN_ASYNCIFY, "0", SDL_HINT_OVERRIDE);
+	}
 #endif
 	if (PortInit(argc, argv) != 0) {
 		return 1;
@@ -1505,6 +1516,13 @@ int main(int argc, char* argv[]) {
 #endif
 	while (WindowIsRunning()) {
 #ifdef __EMSCRIPTEN__
+		/* Wait for the next confirmed 60 Hz simulation tick on the main
+		 * context, never in a render callback or coroutine. Online input
+		 * arrival can resume this loop even when no rAF callback fires. */
+		if (port_netplay_enabled() &&
+		    !port_netplay_wait_frame((uint32_t)port_get_frame_count())) {
+			break;
+		}
 		/* Stall attributor (SSB64_STALL_WATCH=1, off by default).
 		 * Splits each frame's wall time into work done INSIDE wasm
 		 * (PortPushFrame) versus time the browser held the main thread
@@ -1567,7 +1585,7 @@ int main(int argc, char* argv[]) {
 		 * Asyncify while awaiting fetch; they do not advance the frame. Also
 		 * paces the game to ~60Hz since the in-fiber pacer is disabled
 		 * on WASM (see gameloop.cpp). */
-		{
+		if (!port_netplay_enabled()) {
 			if ((frame % 60) == 1) {
 				EM_ASM({ document.title = 'BattleShip f=' + $0; }, frame);
 			}
